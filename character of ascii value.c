#include<stdio.h>
int main()
{
int ascii_val;
printf("enter a ascii value\n");//get the ascii value from user
scanf("%d",&ascii_val);
printf("character of that ascii value %c",ascii_val);//printing the integer as character
return 0;
}
