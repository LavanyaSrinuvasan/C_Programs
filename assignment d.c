#include<stdio.h>
int main()
{
    int a;
    printf("reading using percentage i\n");
    scanf("%i",&a);
    printf("number entered is %i\n",a);// the same value entered as integer
    printf("number entered is %d\n",a);//  the same value entered as decimal
    return 0;
}
